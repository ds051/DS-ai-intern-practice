一、学习整体回顾
 
一个月前，我不熟悉Git版本控制，没有调用大模型API的实战经验，只会编写简单独立Python脚本。经过分阶段练习，从基础文本批量处理脚本起步，逐步完成参数优化、结构化JSON输出改造，最终独立开发完成电机测试数据分析工具，实现从原始脏CSV数据清洗、指标统计、异常识别、AI分析报告生成到结果导出的全自动化流程，产出一套具备工程化规范、可交付使用的完整工具。
 
二、掌握的技术清单
 
1. Python工程开发
- 模块化编程：拆分功能为独立py文件，函数封装、代码复用；
- 命令行参数 argparse ，解除代码硬编码路径，工具通用性大幅提升；
- 文件操作、CSV数据清洗、异常捕获，程序单个文件失败不中断整体流程；
-  .env 密钥管理，区分配置文件与业务代码，保护API密钥，掌握 .gitignore 使用规范。
2. 大模型API开发
- DeepSeek API调用，区分网页对话与程序API调用差异；
- 使用官方 response_format 原生JSON模式，替代单纯prompt约束，提升输出稳定性；
- Token计费理解、请求异常处理，学会控制调用成本。
3. Git版本管理
- 文件新增、提交、推送、移动文件基础操作；
- 建立规范意识：一个功能一次commit，区分代码提交与文档提交；
- 理解公共仓库推送后尽量不重写历史的团队协作规范。
4. 项目交付规范
- 编写标准README，让他人无需询问即可部署运行工具；
- 项目目录整理，区分练习脚本与正式项目代码；
- 工程思维：程序不仅要“能跑”，还要易用、易维护、方便他人接手。
 
三、印象最深的3个问题与解决方案
 
1. 问题：代码路径写死，工具只能本机运行
 
最初开发时直接在代码内写死文件夹路径，更换电脑、换目录运行直接报错。
解决：通过 __file__ 获取脚本基准绝对路径，新增 --input 命令行参数，优先级：命令传入路径 > 默认目录，外部使用者可以自由指定数据文件夹，工具具备通用性。
 
2. 问题：依靠Prompt要求AI返回JSON，经常输出markdown代码块，解析报错
 
早期仅在提示词中要求大模型输出JSON，返回结果不稳定，经常附带```json标记，程序解析崩溃。
解决：查阅DeepSeek官方文档，启用API原生 response_format={"type":"json_object"} 模式，强制模型输出纯净JSON，不再需要手动清洗文本，稳定性显著提升。
 
3. 问题：一次性批量提交大量文件，不符合“一个功能一个commit”规范
 
整理项目文件夹时，所有文件移动操作合并为单条commit，提交记录混乱。
解决：理解行业协作规范，已经推送到远程的历史提交不再强行篡改；后续所有新改动严格拆分：功能代码单独提交、文档修改单独提交，杜绝 git add . 全部提交，养成良好提交习惯。
 
四、对“AI辅助工作”认知变化
 
一个月前理解
 
AI只是问答工具，类似高级搜索引擎，只能用来写零散代码、临时查问题，把AI生成的代码直接复制就能用。
 
现在理解
 
1. AI是辅助工具，不能替代工程思考。AI给出的代码经常存在边界漏洞、硬编码、缺少异常处理，需要自己校验、重构、模块化改造；
2. AI落地核心是自动化流程串联。单纯网页聊天意义有限，真正价值是通过API嵌入程序，把重复人工工作（批量文本处理、数据分析、撰写报告）自动化；
3. 想要稳定使用AI，需要做好约束：结构化输出、异常捕获、流量与成本管控，没有规范的调用很容易出现程序崩溃、不必要的token消耗。
 
五、后续收获与展望
 
从写一次性脚本，转变为面向交付、面向他人使用的工程化思维。后续持续巩固模块化设计、异常处理能力，完善项目演示汇报材料，把本次电机数据分析工具作为完整实战项目，用于面试展示。